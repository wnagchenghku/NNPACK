
# make -j

default: so

.PHONY: dirs clean

dirs:
	/bin/mkdir -p objs/

clean:
	/bin/rm -rf *.so


obj := $(patsubst %.c, %.so, $(wildcard *.c))

so: $(obj)

%.so: %.c
	gcc -m32 -Wl,-hash-style=sysv -fno-stack-protector -fPIC -nostdlib -shared $< -o $@
